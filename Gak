const puppeteer = require('puppeteer');
const ioHook = require('iohook');

(async () => {
  // Abrir Chrome (visible o en background)
  const browser = await puppeteer.launch({
    headless: false // puedes poner true si no quieres ver la ventana
  });

  const page = await browser.newPage();

  // Abre la página que quieres controlar
  await page.goto('https://google.com');

  console.log('Listo. Presiona "+" para enviar "6" a la pestaña.');
      await page.keyboard.press('6');
    }
  });
